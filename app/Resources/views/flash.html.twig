{% for type, messages in app.session.flashBag.all %}
{% if messages|length > 0 %}
<div class="o-layout--flush">
    {% for message in messages %}
        <div class="c-flash c-flash-{{ type }}">
            {{ message }}
        </div>
    {% endfor %}
</div>
{% endif %}
{% endfor %}
